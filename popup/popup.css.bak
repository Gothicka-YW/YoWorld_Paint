/* ===== Transitions & Layout (unchanged) ===== */
.slide-fade-enter-active[data-v-598e593b], .slide-fade-leave-active[data-v-598e593b] {
  transition: all 0.3s cubic-bezier(0.165, 0.84, 0.44, 1);
}
.slide-fade-enter[data-v-598e593b], .slide-fade-leave-to[data-v-598e593b] {
  transform: translateX(25px);
  opacity: 0;
}
.root[data-v-598e593b] {
  width: 450px;
  height: 100%;
  display: flex;
  flex-direction: column;
}

/* ===== Header ===== */
header[data-v-598e593b] {
  font-size: 18px;
  background: #520404;
  color: white;
  padding: 10px;
  text-align: center;
}

/* ===== Tabs row (with divider, now 3 columns) ===== */
nav[data-v-598e593b] {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  border-bottom: 1px solid #eee;
}

/* Tab buttons (accent, hover shadow, focus shadow) */
nav .link button[data-v-598e593b] {
  border-radius: 0;
  outline: none;
  border: none;
  background: #520404;
  color: #fff;
  transition: box-shadow .15s ease, background .15s ease;
}
nav .link button[data-v-598e593b]:hover,
nav .link button[data-v-598e593b]:focus {
  background: #300202;
  color: #fff;
  box-shadow: 0 1px 4px rgba(0,0,0,.15);
}
nav .link.router-link-exact-active button[data-v-598e593b] {
  background: #520404;
  color: #fff;
  box-shadow: inset 0 -2px 0 rgba(255,255,255,.2);
}

/* Footer */
footer[data-v-598e593b] {
  margin-top: auto;
  padding: 20px;
  background: #1b1c1d;
  color: white;
}

/* ===== Content & Spacing ===== */
.content[data-v-4efbad1f] { padding: 20px; }
.switch-container[data-v-4efbad1f] { display: flex; align-items: center; gap: 10px; }
.switch-container #enabled[data-v-4efbad1f] { margin-right: 10px; }
.image[data-v-4efbad1f] {
  margin: 20px 0;
  width: 100%;
  height: 260px;
  border: 1px solid #520404;
  border-radius: 6px;
}
.input-group[data-v-4efbad1f] { display: grid; grid-gap: 15px; padding-bottom: 10px; }
.info[data-v-e57e3f6c] { padding: 20px; }

/* ===== Accent Variables ===== */
:root { --accent: #520404; --accent-hover: #300202; }

/* Primary buttons (Set New Image, etc.) */
button[data-v-4efbad1f] {
  background: var(--accent);
  color: #fff;
  border: none;
  padding: 6px 12px;
  border-radius: 4px;
  cursor: pointer;
  font-family: Georgia, serif !important;
  transition: box-shadow .15s ease, background .15s ease, transform .02s ease;
}
button[data-v-4efbad1f]:hover,
button[data-v-4efbad1f]:focus {
  background: var(--accent-hover);
  color: #fff;
  box-shadow: 0 2px 6px rgba(0,0,0,.18);
}
button[data-v-4efbad1f]:active { transform: translateY(1px); }

/* ===== Ant Design Switch (Enable Direct) ===== */
button.ant-switch[data-v-4efbad1f] {
  background: #888 !important;
  border-color: #888 !important;
  color: #fff;
  transition: background .15s ease, box-shadow .15s ease;
}
button.ant-switch[data-v-4efbad1f]:hover {
  background: #777 !important;
  border-color: #777 !important;
}
button.ant-switch.ant-switch-checked[data-v-4efbad1f] {
  background: var(--accent) !important;
  border-color: var(--accent) !important;
  color: #fff;
}
button.ant-switch.ant-switch-checked[data-v-4efbad1f]:hover {
  background: var(--accent-hover) !important;
  border-color: var(--accent-hover) !important;
}
button.ant-switch[data-v-4efbad1f] .ant-switch-inner { color: #fff; }

/* ===== Sales Boards Tab – tab button styling (kept) ===== */
nav[data-v-598e593b] .link.__sales { align-self: stretch; }
nav[data-v-598e593b] .link.__sales > button {
  display: block;
  width: 100%;
  height: 100%;
  text-align: left;
  padding: 6px 12px 6px 30px;  /* space for pseudo-icon */
  position: relative;
  background: var(--accent);
  color: #fff;
  border: none;
  border-radius: 0;
  cursor: pointer;
  transition: box-shadow .15s ease, background .15s ease;
}
nav[data-v-598e593b] .link.__sales > button:hover,
nav[data-v-598e593b] .link.__sales > button:focus {
  background: var(--accent-hover);
  color: #fff;
  box-shadow: 0 1px 4px rgba(0,0,0,.15);
}
nav[data-v-598e593b] .link.__sales > button::before {
  content: "";
  position: absolute;
  left: 10px; top: 50%;
  transform: translateY(-50%);
  width: 16px; height: 16px;
  border: 2px solid #fff;
  border-radius: 3px;
  opacity: 0.95;
}

/* ===== Blank Sales panel look (not used by grid, safe to keep) ===== */
#panel-sales .panel-sales {
  padding: 20px;
  margin: 0 20px;
  border: 1px dashed #d9d9d9;
  border-radius: 6px;
  background: #fff;
}
#panel-sales .panel-sales h2 { margin: 0 0 8px; font-size: 16px; color: #333; }
#panel-sales .panel-sales .muted { color: #666; font-size: 13px; }

/* === Sales Boards Centering === */
#panel-sales .sales-wrap {
  display: flex;
  flex-direction: column;
  align-items: center;      /* center horizontally */
  justify-content: center;  /* center vertically */
  gap: 12px;
}
#panel-sales .sales-form {
  display: flex;
  flex-direction: column;
  align-items: center;      /* center form inputs */
  gap: 6px;
}
#panel-sales .sales-row {
  display: flex;
  gap: 6px;
  justify-content: center;  /* center url+caption pairs */
}
#panel-sales .sales-actions {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-top: 10px;
}
#panel-sales .sales-preview {
  display: flex;
  justify-content: center;
  margin-top: 12px;
}

/* --- Sales Boards: ensure inputs are visible and sized --- */
#panel-sales .sales-row input {
  background: #fff;
  color: #111;               /* make typed text visible */
  box-sizing: border-box;
}
/* Placeholder color (Chrome/Opera/WebKit) */
#panel-sales .sales-row input::placeholder {
  color: #888;
  opacity: 1;
}
/* Give both fields a consistent width while centered */
#panel-sales .sales-row .sales-url,
#panel-sales .sales-row .sales-cap {
  width: 340px;              /* adjust as you like (300–360 fits the 450px popup) */
  max-width: 100%;
  flex: 0 0 auto;            /* prevent flex from shrinking */
}
